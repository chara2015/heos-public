# config.yml - Heos Folia 配置
# 源代码链接: https://github.com/chara2015/heos
# 开源协议: MIT
# 修改后重启服务器生效。
# 数据文件保存在当前插件目录的 data 文件夹中。

auth:
    # 是否启用登录/注册认证。关闭后玩家无需使用 /login 或 /register。
    enabled: false
    # 服务端消息语言。auto 表示根据运行服务器的电脑系统语言自动选择，也可填写 zh_cn 或 en_us 强制指定。
    # 玩家进入服务器后，提示消息会优先根据玩家客户端游戏语言自动选择。
    language: auto

offline:
    # 在线模式服务器中是否允许非正版玩家进入。关闭后只允许通过正版验证的玩家进入。
    allow-players: false
    # 是否允许离线玩家名称在正版名称规则外额外使用 +、-、. 这三个字符。
    allow-more-username-characters: false
    # 是否允许离线玩家名称使用中文、其他语言字母和数字等 Unicode 字符。（开启后也会包括允许 +、-、. 这三个字符，并且所有正版规则外的字符不强制使用）
    allow-unicode-username-characters: false

login:
    # 玩家进入服务器后必须完成登录/注册的时间限制，单位为秒。
    timeout-seconds: 60
    # 未登录玩家收到登录/注册提醒的间隔，单位为秒。
    reminder-seconds: 10
    # 注册密码允许的最短长度。
    min-password-length: 4
    # 注册密码允许的最长长度。
    max-password-length: 32

security:
    # 同一个 IP 最多允许同时在线的玩家数量。-1 表示不限制。
    max-sessions-per-ip: -1
    # 同 IP 在线数量超过 max-sessions-per-ip 时踢出玩家显示的消息。
    # 这是管理员可自定义文案，不会自动跟随语言文件。
    session-limit-kick-message: The online session limit for this IP has been reached
    # 是否在同一用户名连续登录失败过多时临时锁定该用户名。
    username-failure-lock: false
    # 触发用户名临时锁定前允许的连续失败次数。
    username-failure-limit: 5
    # 用户名触发失败锁定后的持续时间，单位为秒。
    username-failure-lock-seconds: 30
    # 是否在同一 IP 连续登录失败过多时临时锁定该 IP。
    ip-failure-lock: false
    # 触发 IP 临时锁定前允许的连续失败次数。
    ip-failure-limit: 10
    # IP 触发失败锁定后的持续时间，单位为秒。
    ip-failure-lock-seconds: 30

features:
    # 是否启用 Heos 自带白名单检查。
    whitelist: false
    # 是否启用 Heos 自带封禁系统和封禁命令。
    custom-ban: false
    # 是否启用离线/正版玩家数据迁移功能。只在需要迁移玩家数据时开启。
    player-data-migration: false
    # 数据迁移期间临时阻止相关玩家登录的时间，单位为秒。
    migration-ban-seconds: 30

integrations:
    # 是否在玩家列表页脚显示服务器 TPS 信息。
    tps-footer: false
    # TPS 信息刷新间隔，单位为 tick。20 tick 约等于 1 秒。
    tps-footer-delay-ticks: 20
    # 是否启用日志过滤，隐藏登录、注册、改密等敏感命令内容。
    log-filter: false
    # 是否启用 JEI/REI 配方查看器同步。仅在支持的 Minecraft 版本上生效。
    recipe-viewer-sync: false

rules:
    # Show the server rules book when a player joins for the first time.
    enabled: false
    # Require acceptance before the player can continue playing.
    require-agreement: true
    # Separate pages with | and use \n for a line break.
    pages: Welcome to the server!|Please read and accept these rules to continue.
    # Set true after changing rules to clear every existing agreement once.
    update: false
